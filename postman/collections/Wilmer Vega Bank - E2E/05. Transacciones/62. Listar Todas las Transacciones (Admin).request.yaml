$kind: http-request
name: '62. Listar Todas las Transacciones (Admin)'
method: GET
url: '{{baseUrl}}/api/admin/transactions'
order: 62000
headers:
  - key: Authorization
    value: 'Bearer {{token}}'
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test('Status 200', function () { pm.response.to.have.status(200); });
