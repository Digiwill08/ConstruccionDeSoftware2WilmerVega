$kind: http-request
name: '42. Ver Cliente Natural por ID'
method: GET
url: '{{baseUrl}}/api/customers/natural/{{naturalClientId}}'
order: 42000
headers:
  - key: Authorization
    value: 'Bearer {{token}}'
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test('Status 200', function () { pm.response.to.have.status(200); });
