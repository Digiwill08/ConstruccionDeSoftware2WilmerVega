$kind: http-request
name: '48. Ver Cliente Natural Legacy por Documento'
method: GET
url: '{{baseUrl}}/api/customers-legacy/natural/{{naturalDocument}}'
order: 48000
headers:
  - key: Authorization
    value: 'Bearer {{token}}'
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test('Status 200', function () { pm.response.to.have.status(200); });
