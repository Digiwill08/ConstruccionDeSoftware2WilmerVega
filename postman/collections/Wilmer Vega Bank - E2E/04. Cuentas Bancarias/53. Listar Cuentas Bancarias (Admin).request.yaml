$kind: http-request
name: '53. Listar Cuentas Bancarias (Admin)'
method: GET
url: '{{baseUrl}}/api/admin/bank-accounts'
order: 53000
headers:
  - key: Authorization
    value: 'Bearer {{token}}'
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test('Status 200', function () { pm.response.to.have.status(200); });
