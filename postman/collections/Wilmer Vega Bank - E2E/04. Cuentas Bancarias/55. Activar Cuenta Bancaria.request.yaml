$kind: http-request
name: '55. Activar Cuenta Bancaria'
method: PATCH
url: '{{baseUrl}}/api/admin/bank-accounts/{{accountNumber}}/activate'
order: 55000
headers:
  - key: Authorization
    value: 'Bearer {{token}}'
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test('Status 200', function () { pm.response.to.have.status(200); });
