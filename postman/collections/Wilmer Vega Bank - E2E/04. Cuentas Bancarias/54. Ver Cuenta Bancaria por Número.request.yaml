$kind: http-request
name: '54. Ver Cuenta Bancaria por Número'
method: GET
url: '{{baseUrl}}/api/admin/bank-accounts/{{accountNumber}}'
order: 54000
headers:
  - key: Authorization
    value: 'Bearer {{token}}'
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test('Status 200', function () { pm.response.to.have.status(200); });
