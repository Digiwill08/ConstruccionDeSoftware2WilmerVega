$kind: http-request
name: '56. Desactivar Cuenta Bancaria'
method: PATCH
url: '{{baseUrl}}/api/admin/bank-accounts/{{accountNumber}}/deactivate'
order: 56000
headers:
  - key: Authorization
    value: 'Bearer {{token}}'
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test('Status 200', function () { pm.response.to.have.status(200); });
