$kind: http-request
name: '45. Ver Cliente Empresa por Documento'
method: GET
url: '{{baseUrl}}/api/customers/company/document/{{companyDocument}}'
order: 45000
headers:
  - key: Authorization
    value: 'Bearer {{token}}'
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test('Status 200', function () { pm.response.to.have.status(200); });
