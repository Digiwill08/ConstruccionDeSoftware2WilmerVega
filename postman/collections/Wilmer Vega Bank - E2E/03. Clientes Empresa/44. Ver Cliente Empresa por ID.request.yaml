$kind: http-request
name: '44. Ver Cliente Empresa por ID'
method: GET
url: '{{baseUrl}}/api/customers/company/{{companyClientId}}'
order: 44000
headers:
  - key: Authorization
    value: 'Bearer {{token}}'
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test('Status 200', function () { pm.response.to.have.status(200); });
