$kind: http-request
name: '51. Ver Empresa Legacy por Documento'
method: GET
url: '{{baseUrl}}/api/customers-legacy/company/{{companyDocument}}'
order: 51000
headers:
  - key: Authorization
    value: 'Bearer {{token}}'
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test('Status 200', function () { pm.response.to.have.status(200); });
