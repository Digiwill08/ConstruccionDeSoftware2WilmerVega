$kind: http-request
name: '50. Listar Empresas (Legacy)'
method: GET
url: '{{baseUrl}}/api/customers-legacy/company'
order: 50000
headers:
  - key: Authorization
    value: 'Bearer {{token}}'
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test('Status 200', function () { pm.response.to.have.status(200); });
